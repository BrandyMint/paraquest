defaults: &defaults
  title: Domestic
  default_url_options: &default_url_options
    host: <%= ENV['APP_HOST'] || 'localhost' %>

development:
  <<: *defaults
test:
  <<: *defaults
production:
  <<: *defaults
staging:
  <<: *defaults
